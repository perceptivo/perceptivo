# .readthedocs.yml

version: 2

sphinx:
  configuration: docs/conf.py

formats:
   - pdf

build:
  os: ubuntu-20.04
  apt_packages:
    - pulseaudio
  tools:
    python: "3.9"

python:
  install:
    - method: pip
      path: .
      extra_requirements:
        - docs